"use client";

// SEO is handled by Next.js route-level `metadata` exports (see src/app/*/page.tsx).
// This shim keeps the original component API inert on the client.
const SEO = () => null;

export default SEO;
